# 旗帜编织逆向器 - 开发踩坑记录

## 一、设置窗口（SettingsDialog）

### 1. 字体缩放
- **坑**：通过 `parent.font().pointSize()` 计算字体大小，在高DPI下拿到的值不准确
- **正解**：读取主窗口 `_scale` 属性，用 `base_fs = 13 * scale` 计算
- **坑**：控件高度用 `min-height: 26 * scale` 会导致 QComboBox 在 250% 下变成 85px 高的长方形
- **正解**：去掉 `min-height`，只用 `padding: 1px Npx`，让高度由文字+padding 自然撑开

### 2. QComboBox 样式
- **坑**：不自定义 `::drop-down` 的话，Windows 默认下拉按钮又丑又占位
- **正解**：设置 `::drop-down { width: 22px; border: none; }`，`padding-right` 预留箭头空间
- **坑**：`padding` 四参数写法 `上 右 下 左` 必须写对，否则文字被箭头压住

### 3. 排版间距
- **坑**：`addStretch()` 放在页面底部会把所有内容挤到顶部，中间出现大片空白
- **正解**：去掉 `addStretch`，改用 `QScrollArea` 包裹内容区，内容多就滚动
- **坑**：`body.addWidget(stack, 1)` 的 stretch=1 会强制内容区撑满，选项之间被拉散
- **正解**：用 `QScrollArea` 替代直接 addWidget，内容不撑满
- **坑**：行间距 `spacing(3)` 太挤像压缩饼干，`spacing(16)` 又太散
- **正解**：行间距 8px，组间距 6px，页面边距 18/12/18/8

### 4. 去除多余元素
- **坑**：`QDialog` 默认标题栏有 `?` 帮助按钮
- **正解**：`setWindowFlags(Qt.Window | Qt.WindowTitleHint | Qt.WindowCloseButtonHint)`
- **坑**：导航栏 emoji 在不同字体下渲染不一致
- **正解**：全部去掉，纯文字导航

### 5. 重启提示逻辑
- **坑**：只检测"是否有需重启的项变更"不够，用户不知道具体改了什么
- **正解**：遍历对比新旧值，列出变更项名称，弹 `QMessageBox.question` 询问是否立即重启
- **坑**：导入器没有需重启项，但逻辑混在一起导致也弹窗
- **正解**：`if self.config_name == "trainer"` 才检测

## 二、进度条与启动流程

### 1. 假进度 vs 真实进度
- **坑**：纯假进度（固定时间到85%）与真实启动速度不匹配，出现"进度条满了程序没开"或"程序开了进度条才一半"
- **正解**：训练器/导入器在关键节点写入 `.trainer_progress` / `.importer_progress` 文件，加载界面读取真实进度，假进度仅作兜底（8秒到70%）
- **坑**：假进度权重 `fake * 0.3` 导致真实进度写入后进度条几乎不动
- **正解**：用 `max(real, fake)`，真实进度一旦写入立即生效

### 2. 进度文案
- **坑**：文案过于概括（"正在启动..."）用户不知道卡在哪
- **正解**：分阶段文案：UI初始化 → PyTorch框架 → ViT模型 → 参数分析 → 优化器编译 → 完成

### 3. 进度先慢后快
- **坑**：假进度到85%要20秒，最后ready后0.4秒冲到100%，感官上先卡后冲
- **正解**：假进度8秒到70%，ready后1.5秒线性到100%

## 三、重启机制

### 1. 联动关闭
- **坑**：`os.execl()` 在 Windows 下不生效
- **正解**：`subprocess.Popen([sys.executable] + argv)` + `sys.exit(0)`
- **坑**：重启只关训练器，没关导入器，导致新进程检测到导入器还在就跳过启动流程
- **正解**：重启前依次调用 `_write_closing_signal()` → `_write_quit_signal()` → `_cleanup_group_lock()`，走完整联动关闭流程

### 2. 单实例锁
- **坑**：重启时旧进程没释放锁，新进程启动报"已有实例运行"
- **正解**：重启前 `_cleanup_group_lock()` 释放锁，且设置 `_force_quit = True` 跳过关闭确认弹窗

### 3. 运行时参数过滤
- **坑**：`subprocess.Popen([sys.executable] + sys.argv)` 把 `--left-half`、`--session-dir` 也传给新进程，新进程直接跳到主窗口跳过加载条
- **正解**：重启时过滤掉 `--left-half`、`--right-half`、`--session-dir`、`--data`，让新进程走完整启动流程

## 四、跨进程通信

### 1. 信号文件
- **坑**：只用一个 `.ready` 文件不够，无法区分"正在关闭"和"已退出"
- **正解**：分两级信号：
  - `.trainer_closing` — 训练器正在关闭（导入器收到后开始准备关闭）
  - `.trainer_quit` — 训练器已退出（导入器收到后立即退出）
- **坑**：信号文件写入后没及时被检测，导入器关闭延迟
- **正解**：导入器用 100ms 定时器轮询检测信号文件

### 2. 进度信号
- **坑**：训练器和导入器都写进度但格式不统一
- **正解**：统一格式 `"{value}\n{status}"`，加载界面分别读取后合并

## 五、日志生成

### 1. 异常处理
- **坑**：`except ImportError` 兜不住 psutil 的 DLL 加载失败、权限不足等
- **正解**：所有监测代码块用 `except Exception`，且移除复杂的 ctypes 后备方案
- **坑**：`subprocess.run(capture_output=True)` 在旧版 Python 不支持
- **正解**：`subprocess.run(stdout=PIPE, stderr=PIPE)`

### 2. 日志路径
- **坑**：默认存项目根目录，文件散落
- **正解**：默认存 `data/` 目录，配置值用 `"default"` 归一化，显示时展开为真实路径

### 3. 自动检测值显示
- **坑**：GPU/内存/Worker 选"自动"时只显示"自动"二字，用户不知道实际值
- **正解**：检测实际值并显示，如 `自动（12GB）`、`自动（7核）`

## 六、样式相关

### 1. 按钮
- **坑**：`setFixedWidth` 导致长文字被截断
- **正解**：`setMinimumWidth`，让按钮根据文字自动撑开
- **坑**：确定按钮和取消按钮样式不一致
- **正解**：确定按钮用蓝色主色 `#1a73e8`，取消按钮用默认描边样式

### 2. 颜色按钮
- **坑**：颜色选择按钮变成高长方形
- **正解**：固定 56×56px 正方形，本地样式覆盖继承的 min-height

### 3. 窗口标志
- **坑**：`Qt.Window` 默认带帮助按钮（?）
- **正解**：`Qt.Window | Qt.WindowTitleHint | Qt.WindowCloseButtonHint`

## 七、通用经验

### 1. 改动前先读代码
- **坑**：凭记忆改代码，old_string 匹配不上
- **正解**：改之前先 Read 确认当前代码内容

### 2. 语法检查
- **坑**：改完不检查就提交，运行时才发现 NameError
- **正解**：每次改完 `python -m py_compile` 检查

### 3. 不要过度设计
- **坑**：每页搞多个子 GroupBox（"基础设置"+"偏好设置"），层级杂乱
- **正解**：一页一个 GroupBox，化繁为简

### 4. 缩放因子统一
- **坑**：有的地方用 `base_fs/10`，有的用 `scale`，导致缩放不一致
- **正解**：统一用 `self._scale` 作为缩放因子

## 八、UI 框架迁移（Tkinter → PyQt5）

### 1. 框架选型
- **坑**：训练导入器最初基于 Tkinter，稳定性差，自适应窗口实现困难
- **正解**：迁移到 PyQt5，控件更丰富，信号槽机制更成熟
- **教训**：GUI 框架选型应优先考虑成熟度，不要被"简单"误导

### 2. 迁移过程
- **坑**：迁移过程中遗留大量 Tkinter 思维代码，导致渲染窗口报错
- **正解**：完全弃用 Tkinter，PyQt5 架构搬迁完成后继续清理屎山代码

## 九、窗口缩放与比例适配

### 1. 多种缩放方式
- **坑**：曾尝试三种缩放方式，逻辑复杂相互冲突
- **正解**：简化为两种（宽模式/窄模式），降低维护成本

### 2. Windows Snap 半屏比例
- **坑**：训练导入器在 Windows snap 后窗口比例不是半屏，比例限制失效
- **坑**：21:9 等超宽比例下限制失效长期存在
- **正解**：调整最大最小比例，增加 21:9 到 4:3 的比例适配
- **教训**：限制函数要考虑极端比例，不能只针对 16:9 优化

### 3. 实现左右分屏
- **坑**：PyQt5 自带的窗口管理 API 在 Windows 下效果不稳定
- **正解**：使用 Windows 2000 时期就自带的 `AttachThreadInput + BringWindowToTop` 实现
- **教训**：有时老 API 反而更可靠，不要迷信现代框架

### 4. 窄模式布局
- **坑**：窄模式下空间利用率低
- **正解**：调整窄模式显示方式，最大化利用空间

## 十、训练模型与训练流程

### 1. PEFT 辅助训练
- **坑**：启用 peft 后疯狂报错
- **正解**：让 peft 不报错后又导致导入器渲染窗口出问题，反复调优
- **教训**：第三方库引入要谨慎评估兼容性

### 2. 两阶段训练策略
- **坑**：直接解冻全部 ViT 微调，容易破坏预训练权重
- **正解**：冻结阶段（前 1/3 轮次）只训练分类头 → 微调阶段（后 2/3 轮次）解冻最后 4 层
- **正解**：微调阶段学习率降一个数量级（0.0001 → 0.00001）

### 3. GPU 保护机制
- **坑**：长时间训练可能损坏 GPU
- **正解**：使用 `pynvml` 监测 GPU 温度，超过 80°C 自动停止训练

### 4. 颜色混淆问题
- **坑**：以下颜色对容易混淆：
  - 橙色 vs 棕色（暖色调接近）
  - 粉色 vs 品红色（粉紫接近）
  - 灰色 vs 黑色（暗色接近）
  - 浅灰色 vs 白色（亮色接近）
  - 青色 vs 浅蓝色（冷色接近）
- **正解**：使用纠偏生成器，控制变量法生成变体样本

### 5. 渲染阴影压缩颜色差异
- **坑**：旗帜下半部分阴影较重，颜色被乘以约 0.84 的系数，进一步压缩颜色差异
- **正解**：同步 Minecraft 官方旗帜渲染颜色与阴影细节；在阴影区域放置不同颜色图案训练

### 6. 图案对称性混淆
- **坑**：左右对称图案（bl/br、tl/tr）、上下对称图案（bs/ts、bt/tt）容易混淆
- **正解**：图案纠偏模式下，保持颜色不变，遍历图案类型生成对比样本

### 7. 空槽位识别
- **坑**：模型可能将空槽位预测为有图案（假阳性），或有图案的槽位被跳过（假阴性）
- **正解**：增加不同图案层数的样本，空槽位样本占比 20%~30%

### 8. 第一个模型效果不佳
- **坑**：2026/4/13 训练第一个旗帜文件效果不佳
- **正解**：2026/4/30 训练出第一个具有可靠逆向能力的预训练模型
- **正解**：2026/5/2 训练出第一个真正可用的预训练基准模型
- **教训**：训练数据质量和纠偏策略比单纯增加数据量更重要

## 十一、双程序联动机制

### 1. 启动捆绑
- **坑**：训练器和导入器独立启动，用户操作繁琐
- **正解**：启动上正式进行捆绑，统一入口

### 2. 联动关闭
- **坑**：关闭一个程序时另一个还在运行，资源未释放
- **正解**：走完整联动关闭流程：`_write_closing_signal()` → `_write_quit_signal()` → `_cleanup_group_lock()`

### 3. 渲染窗口联动 bug
- **坑**：peft 修复后导致导入器渲染窗口出问题
- **正解**：分阶段验证每个改动的影响范围

## 十二、代码质量管理

### 1. 屎山代码积累
- **坑**：AI 改代码时经常生成功能重复的模块（如 `main_window.py` 重复实现进度条逻辑）
- **正解**：发现重复立即删除，让程序简洁高效
- **教训**：定期铲除屎山，避免函数互相矛盾

### 2. 竞态条件
- **坑**：过多计时器和异步函数导致竞态问题，布局异常
- **正解**：运用减法思维，移除大量屎山函数/变量/计时器

### 3. 函数互相打架
- **坑**：多个函数实现相似功能，互相覆盖行为
- **正解**：去重，统一入口

### 4. 改动前先读代码
- **坑**：凭记忆改代码，old_string 匹配不上
- **正解**：改之前先 Read 确认当前代码内容

### 5. 语法检查
- **坑**：改完不检查就提交，运行时才发现 NameError
- **正解**：每次改完 `python -m py_compile` 检查

### 6. SPP 意外崩溃
- **坑**：开发过程中 SPP 直接拿下系统，造成重大损失
- **教训**：重要代码及时备份，操作系统稳定性不能完全依赖

## 十三、报错与异常处理

### 1. 异常捕获范围
- **坑**：`except ImportError` 兜不住 psutil 的 DLL 加载失败、权限不足等
- **正解**：所有监测代码块用 `except Exception`

### 2. 子进程 API 兼容
- **坑**：`subprocess.run(capture_output=True)` 在旧版 Python 不支持
- **正解**：`subprocess.run(stdout=PIPE, stderr=PIPE)`

### 3. 外部监督机制
- **坑**：训练过程出错后无任何反馈
- **正解**：引入外部程序监督报错管理机制，捕获并显示错误

### 4. stdout/stderr 重定向
- **坑**：GUI 模式下 `sys.stdout` 为 None，print 报错
- **正解**：启动时检查并重定向到 `os.devnull`

## 十四、DPI 与文字缩放

### 1. 高 DPI 适配
- **坑**：250% DPI 下控件变形（如 QComboBox 变成 85px 高）
- **正解**：去掉 `min-height`，让高度由文字 + padding 自然撑开

### 2. 全局自适应缩放
- **坑**：各窗口字体大小不一致
- **正解**：为所有窗口统一增加自适应 DPI 文字缩放功能

### 3. UI 缩放计算
- **坑**：单纯基于屏幕宽高计算缩放比例，忽视 DPI
- **正解**：`min(raw, 1.4) * dpi_factor * 1.1`，结合屏幕尺寸和 DPI

## 十五、文件传输与数据格式

### 1. .mbtl 文件格式
- **坑**：早期通过临时文件传递旗帜数据，效率低
- **正解**：启用 `.mbtl` 专用文件格式，更高效解决问题

### 2. 图层中文显示
- **坑**：图案类型直接显示英文标识，用户不直观
- **正解**：制作中文字典 `type_zh`，对应翻译展示

## 十六、调试与版本管理

### 1. Debug 模式
- **坑**：默认显示大量命令提示符信息，正常用户不需要
- **正解**：增加"debug 模式"选项，让用户选择是否显示

### 2. 版本号管理
- **坑**：版本号管理混乱，难以追溯
- **正解**：正式确定版本号体系（如 v0.5 beta1）

### 3. 模型版本管理
- **坑**：训练模型覆盖保存，丢失历史版本
- **正解**：模型文件按时间戳命名，如 `banner_vit_model_20260511_005605.pth`

## 十七、训练数据生成

### 1. 批量生成 vs 手动编辑
- **坑**：仅靠批量生成无法覆盖边缘案例
- **正解**：批量生成基础数据 + 手动编辑补充薄弱图案

### 2. 杜绝重复旗帜
- **坑**：纠偏生成可能产生与已有旗帜完全相同的结果
- **正解**：开启"杜绝重复旗帜"选项，跳过重复结果

### 3. 控制变量法纠偏
- **坑**：随机生成难以针对性纠偏
- **正解**：基于控制变量法，保持部分属性不变，只变化需要纠偏的属性

## 十八、模型保存与加载

### 1. 保存假模型
- **坑**：`save_model` 只检查 `self.model is None`，未训练的模型（初始化但未训练）也能保存
- **正解**：增加 `not getattr(self, "_model_trained", False)` 检查，未训练时提示"模型尚未训练，无法保存"

### 2. 模型架构不匹配
- **坑**：训练器保存模型时只保存 `state_dict()`，不保存架构信息；识别器加载时用默认架构 `ViT()`（vit_b_16），若训练器用了 vit_l_16 等其他架构，加载时因 key 不匹配报错
- **正解**：保存时改为字典格式 `{'model_state_dict': ..., 'model_arch': ...}`；识别器加载时读取 `model_arch` 字段，用正确架构创建 ViT；旧格式（纯 state_dict）回退到默认架构

### 3. 路径选择失效
- **坑**：`save_model`/`load_model` 使用相对路径 `"models/model_file"`，程序从其他目录启动时路径错误
- **正解**：统一使用 `resolve_app_path("models/model_file")` 解析为相对于程序根目录的绝对路径

### 4. training_backup.mbtl 位置错误
- **坑**：训练数据备份 `training_backup.mbtl` 保存在 `models/model_file/` 目录中，与模型权重混放
- **正解**：移到 `training_datas/` 目录，model_file 目录只保存 .pth 模型权重文件

## 十九、文件结构与命名

### 1. 中文文件名报错
- **坑**：程序文件和图片使用中文名称（如"旗帜训练器.pyw"），可能导致路径解析错误
- **正解**：全部重命名为英文（trainer.pyw、importer.pyw、trainer.png、importer.png）

### 2. config/ 目录混合配置与程序
- **坑**：`config/` 目录同时存放 JSON 配置文件和可执行程序（error_reporter.pyw、exit.pyw），概念混乱
- **正解**：程序移到 `scripts/` 目录，`config/` 只保留配置数据（JSON 文件）

### 3. models 目录结构不清
- **坑**：`vit_model.py`（模型架构）直接放在 `models/` 根目录，与 `model_file/`（权重）平级
- **正解**：创建 `models/structures/` 子目录，vit_model.py 移入；更新所有 `from models.vit_model` → `from models.structures.vit_model`

### 4. 菜单栏不统一
- **坑**：训练器有"训练(&T)"菜单，导入器没有；"设置"直接放在菜单栏上而非作为菜单
- **正解**：三个器统一为 文件/编辑/查看/设置/帮助 五个菜单；训练操作归入编辑菜单；设置改为菜单形式

## 二十、窗口管理与单实例

### 1. 置顶窗口导致设置崩溃
- **坑**：`always_on_top` 功能开启后，训练器/导入器调用 `setWindowFlags(Qt.WindowStaysOnTopHint)` 强制置顶，遮挡设置窗口（子进程），用户无法操作设置窗口，看似"崩溃"
- **正解**：完全删除置顶功能，移除相关 UI、配置项和处理逻辑

### 2. help 窗口多实例
- **坑**：help 窗口未限制打开数量，可重复启动
- **正解**：使用 Windows Mutex 机制（`Global\BannerToolHelpSingleInstance`），创建全局互斥体确保只能打开一个实例

### 3. 训练器/导入器快速双击启动多实例
- **坑**：快速点击可能启动多个训练器/导入器实例
- **正解**：使用 Windows Mutex 串行化实例检查，消除竞态条件

### 4. 错误窗口可调整大小
- **坑**：错误报告窗口可调整大小，影响用户体验
- **正解**：固定尺寸（短文本 460×自适应；长文本 560×380，按 scale 缩放）

### 5. help 窗口缩放不同步
- **坑**：help 窗口字体大小与设置窗口不一致，250% 缩放下太小
- **正解**：对齐 scale 公式 `min(ui_scale * 1.25, 2.5)`，与训练器/导入器一致

## 二十一、Tab2 序列图组训练

### 1. 功能区混乱
- **坑**：Tab2 原布局为"训练参数+训练控制+数据列表+详情预览"四组，功能区混乱
- **正解**：参照 Tab1 结构重写为五组：模型设置+总进度+训练中图片+数据列表+图案组成

### 2. snap 功能失效
- **坑**：Tab2 的 4 个 splitter 未连接 `splitterMoved` 信号，导致拖动时不 snap、不约束、不保存
- **正解**：为所有 splitter 连接信号，复用 `_snap_to_other` 逻辑（narrow 模式下 main_splitter ↔ list_splitter 互相 snap）

### 3. 图片和渲染图显示
- **坑**：原图和渲染图分开显示，用户无法对比
- **正解**：training_group 并排显示上传识别图（左）和旗帜渲染图（右），便于对比

### 4. 批次大小输入框无法修改
- **坑**：`auto_resource_alloc=True` 时调用 `self.batch_spin.setEnabled(False)` 禁用了输入框
- **正解**：移除 `setEnabled(False)`，自动分配模式下仍可手动修改

### 5. 工作区布局脏数据
- **坑**：`workspace_layout.json` 混合新旧格式（如 `content: [0, 1077]` 含 0 值），Tab2 出现空数组（如 `wide.tab2.sub2: []`）
- **正解**：清理脏数据；`_apply_graphic_sizes` 返回成功恢复的 splitter 集合，对未恢复的 splitter 应用 1:1 默认值

## 二十二、深色模式与主题适配

### 1. 部分窗口未适配深色模式
- **坑**：error_reporter.pyw、exit.pyw 等窗口未实现主题读取和样式适配
- **正解**：为所有窗口添加主题读取和条件样式设置，统一调用 `apply_theme`

### 2. CPU/GPU 切换和模型加载失败
- **坑**：DLL 冲突导致 bdor.pyw 的 CPU/GPU 切换和模型加载功能失效
- **正解**：恢复顶层 torch imports，避免局部导入引发 DLL 冲突

### 3. Tab2 占位文字颜色硬编码
- **坑**：Tab2 占位文字颜色硬编码，不适配深浅色模式
- **正解**：移除硬编码颜色，通过 `_apply_theme_to_window` 动态设置

## 二十三、性能优化

### 1. _sync_theme_from_config 频繁 IO
- **坑**：`_sync_theme_from_config` 每次都读取配置文件，频繁不必要的 IO
- **正解**：检查文件 mtime，仅在文件修改后才重新加载配置

### 2. import torch 阻塞 UI
- **坑**：`import torch` 耗时 5-15 秒，阻塞 UI 加载
- **正解**：加载进度条中先更新到 0.05 并显示"正在加载PyTorch深度学习框架..."，再执行 import

### 3. splitter 尺寸未就绪
- **坑**：`_enforce_splitter_bounds` 在窗口未显示时执行，splitter 无实际尺寸，约束失效
- **正解**：用重试机制，每 30ms 检查一次，最多重试 5 次直到 splitter 有实际尺寸

## 二十四、错误处理统一

### 1. 多个报错窗口
- **坑**：bdor.pyw 的 `_show_message` 在 error 类型时既调用 `report_error`（触发 error_reporter.pyw）又显示 `ThemedMessageBox`，出现两个报错窗口
- **正解**：error 类型只走 `report_error` → `error_reporter.pyw`，`return` 不再弹 `ThemedMessageBox`；错误统一由 error_reporter.pyw 显示（含导出日志功能）

### 2. 所有报错必须生成日志
- **坑**：部分报错未生成日志文件，难以追溯
- **正解**：通过 `report_error` 和全局 `sys.excepthook` 捕获所有错误，写入 log 目录

### 3. 类型注释弃用警告
- **坑**：PyQt5 类型存根文件使用旧类型注释语法，类型检查器报警告
- **正解**：创建 `pyrightconfig.json`，禁用类型检查并忽略 site-packages 目录

## 二十五、总结

### 核心教训

1. **减法思维优先**：遇到问题先想能否删除/简化，而非添加更多代码。置顶功能、屎山函数、重复模块都应直接删除而非修补。

2. **路径用绝对路径**：所有文件路径统一用 `resolve_app_path()` 解析为绝对路径，避免相对路径在不同工作目录下失效。

3. **模型保存要带元数据**：保存模型时不能只存 `state_dict()`，必须包含架构信息（`model_arch`），否则加载时架构不匹配。

4. **错误处理要统一**：所有错误走统一的 `error_reporter.pyw` 通道，不要程序自己弹多个报错窗口。

5. **文件结构要分层**：配置文件（config/）、可执行程序（scripts/）、模型权重（models/model_file/）、模型架构（models/structures/）、训练数据（training_datas/）各归各位。

6. **中文命名是隐患**：程序文件、图片文件一律用英文名，中文名在路径解析、版本控制中是隐患。

7. **信号连接要完整**：每个 splitter 都必须连接 `splitterMoved` 信号，否则 snap、约束、保存全部失效。

8. **改前先读，改后检查**：每次修改前 Read 确认当前代码，修改后 `py_compile` 检查语法。

### 常见陷阱清单

- `setEnabled(False)` 禁用控件后用户无法操作 → 检查是否有必要禁用
- 相对路径在不同工作目录下失效 → 用 `resolve_app_path()`
- `state_dict()` 不含架构信息 → 保存为字典 `{'model_state_dict':..., 'model_arch':...}`
- `setWindowFlags(WindowStaysOnTopHint)` 遮挡子进程窗口 → 删除置顶功能
- 未连接 `splitterMoved` 信号 → snap 和约束失效
- `except ImportError` 兜不住 DLL 加载失败 → 用 `except Exception`
- `min-height` 在高 DPI 下导致控件变形 → 去掉 `min-height`，用 padding 撑开
- 假进度与真实进度不匹配 → 用 `max(real, fake)`，真实进度优先
